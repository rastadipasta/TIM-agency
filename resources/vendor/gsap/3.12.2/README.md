# GSAP 3.12.2

Vendored from the existing cdnjs URLs to avoid third-party JavaScript requests.
The original upstream license headers are preserved in both files.

Sources:
- https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js
- https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js

Both files were verified against the SHA-384 integrity values already present in
the site HTML. Keep these integrity attributes when updating the libraries.
